#!/bin/bash
# 清理所有占用 8001-8004 端口的进程

echo "🔍 检查端口占用情况..."

for port in 8001 8002 8003 8004; do
    pid=$(lsof -ti:$port)
    if [ -n "$pid" ]; then
        echo "  端口 $port 被进程 $pid 占用，正在终止..."
        kill -9 $pid
        echo "  ✓ 已终止进程 $pid"
    else
        echo "  端口 $port 未被占用"
    fi
done

echo "✅ 端口清理完成"
