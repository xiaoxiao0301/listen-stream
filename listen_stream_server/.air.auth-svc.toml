root = "."
testdata_dir = "testdata"
tmp_dir = "tmp"

[build]
  cmd = "go build -mod=vendor -o ./tmp/auth-svc ./auth-svc/cmd/server/"
  bin = "./tmp/auth-svc"
  full_bin = "PORT=8001 ./tmp/auth-svc"
  include_dir = ["auth-svc", "shared"]
  exclude_dir = ["tmp", "vendor", "testdata", "listen_stream_admin", "listen_stream_client"]
  include_ext = ["go"]
  exclude_regex = ["_test\\.go"]
  delay = 500
  stop_on_error = true
  rerun = false
  rerun_delay = 500

[log]
  time = true
  main_only = false

[color]
  main = "magenta"
  watcher = "cyan"
  build = "yellow"
  runner = "green"

[misc]
  clean_on_exit = true
